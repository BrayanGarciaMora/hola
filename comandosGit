--canviara el nombre a un archivo. 
mv
--cambiar el nombre a un archivo
rm 

--para clonar un repositorio
git clone [porner el nombre del respositorio

-- ir a buscar y jalar  para bajar los cambios que se han hecho en el repositorio remot
git fetch 

-- para actualiazar los cambios
git pull:

---para subir acambios al repositorio
git pull origen master [nombre del repositorio.

